/** @type {import('next').NextConfig} */
const nextConfig = {
  // No proxy needed in v2 — Next.js API routes handle everything.
  // Data lives in data/ at repo root, read directly by fs-data.ts.
};

module.exports = nextConfig;
